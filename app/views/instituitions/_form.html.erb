<%= form_for(@instituition) do |f| %>
  <% if @instituition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@instituition.errors.count, "error") %> prohibited this instituition from being saved:</h2>

      <ul>
      <% @instituition.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
